# Public Interfaces

Public WatchNT interfaces will be documented here as they are implemented.

The first milestone intentionally defines no domain interfaces. The shared `Content`, `KnowledgeObject`, storage, pipeline, AI provider, exporter, retrieval, and plugin interfaces are scheduled for later milestones in dependency order.
